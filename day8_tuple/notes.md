tuple are immutable - meaning you cannot directlty change, add or delete individual elelment like u can in a list 

imp:

elem = ('apple')  ............this is not tupple it is a string

to get tuple with single element we need to use , 

elem = ('apple',)